# this function is supposed to automatically determine if a variable is metric or not
# and then use the mean or median on the larger (census) sample to account for lacking
# details in the survey sample.

mean.for.regression <- function(mean.reg, censusdata){

}
